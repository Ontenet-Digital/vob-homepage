<div class="page-shell page-shell--narrow wrap">
  <div class="page-head">
    <span class="eyebrow">Vision of the Bible</span>
    <h1>{{ page.title }}</h1>
  </div>
  <div class="rte">{{ page.content }}</div>
</div>

{% schema %}
{ "name": "Page", "settings": [] }
{% endschema %}
